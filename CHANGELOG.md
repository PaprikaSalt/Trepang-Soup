# 更新日志

本项目版本遵循语义化版本号。每次更新都会在这里记录，并在形成发行版时同步发布到
GitHub Releases。

## [1.1.0] - 2026-07-31

### 变更

- 游戏中的汤面只保留正文，移除可能泄露关键信息的题目大标题和 AI 推理进度预测。
- 首页与游戏房间的在线状态统一显示为“游戏服务器已连接”。
- 删除创建、加入、对局记录、题库入口、题目来源、难度、风格、结论及提示按钮下方的功能说明，
  并同步收紧按钮布局。
- 完成全员续局投票、撤回同意、生成状态、失败恢复和 `room.restarted` 服务端状态机，客户端
  结算页现在可直接在原房间开始下一轮。
- AI 房间续局沿用难度和风格重新生成，私人题库续局继续使用近期排除策略抽取新题。
- 续局期间保留房间号、成员、房主、会话和事件序号，同时原子清空上一轮问答、讨论、提示、
  时间线与结算状态。
- 结算房间不再经过 5 秒投递宽限后删除，改为全员离线达到 idle 超时后清理。

### 修复

- 修复开局后加入成功却停留在大厅的问题，客户端现在按首次权威快照的房间阶段导航。
- 多人连续提问时，普通问答不再向 DeepSeek 累积历史回答，避免模型自行复盘并补全汤底。
- 主持问答改为仅由模型判定 `answerType`，玩家可见文案由服务端固定生成，并强化提示注入、
  角色切换、总结和变相复述的禁止规则。
- 公共提示会拒绝逐字包含汤底或关键事实的输出；接近正确的结论不再公开缺失事实和模型自由
  反馈。

### 验证

- 新增开局后加入的后端回归测试，确认新玩家收到 `playing` 快照、当前汤面和完整成员列表。
- 客户端模拟模式已走通结算、全员投票、准备下一题和自动进入同一房间下一局。
- 后端覆盖全员投票、撤回、加入/离开成员、命令幂等、AI 失败、私人题库换题、生成中 admission
  拒绝、关闭竞态、旧轮慢提示隔离和真实 WebSocket 续局流程。
- 71 项后端自动化测试、Ruff、格式检查、mypy 和客户端生产构建通过；双客户端传输冒烟完成
  结算、两票同步、生成状态和同房间第二轮，真实 DeepSeek 独立冒烟通过。
- 新增多人历史拼接、提示注入、模型额外字段泄密、公共提示逐字泄密和结论反馈泄密测试。

### 文档

- 新增续局投票与开局后加入协议，明确后端命令、事件、快照、房间清理和验收用例。
- 记录当前公网域名入口的未备案拦截证据，以及备案或公网 IP 非标准端口两种恢复方案。
- 新增 2C2G 性能基线、容量估算、单 worker 约束及公网保护建议。
- 新增公网部署清单，明确邀请制测试与完全公开部署的不同就绪门槛。

## [1.0.0] - 2026-07-30

### 正式发布

- 客户端版本提升至 `1.0.0`，作为首个面向好友分发的正式版本。
- 使用 `client/icon.png` 重新生成 Windows 可执行文件、安装器、卸载器及各尺寸应用图标。
- 保留并纳入项目 README 的人工精简修改。
- 正式版继续通过 `https://api.ljy32.cn` 与 `wss://api.ljy32.cn` 连接公网服务。

### 验证

- 客户端类型检查、生产构建及 Windows x64 NSIS 安装包构建通过。
- 生产资源确认使用 v1.0.0 客户端标识、公网 HTTPS 服务地址和新版应用图标。

## [0.2.2] - 2026-07-30

### 变更

- Windows 正式构建改为通过 Caddy 公网入口 `https://api.ljy32.cn` 连接服务端。
- 房间 WebSocket 随 HTTPS 入口自动使用 `wss://api.ljy32.cn`，不再直接暴露或访问
  Uvicorn 的本机 8787 端口。

### 验证

- 公网 HTTPS 健康检查通过，服务端版本 `0.2.0`、协议版本 `1`。
- 两个独立客户端通过公网 HTTPS/WSS 完成建房、加入、开局、聊天室、正式问答、公共提示
  和同步结算全流程。
- 客户端类型检查、生产构建及 Windows x64 NSIS 安装包构建通过。

## [0.2.1] - 2026-07-30

### 新增

- Windows 正式构建默认连接 `http://ljy32.cn:8787`，开发模式仍默认连接本机服务。
- 房间连接意外中断后按 1、2、4、8、15 秒自动重试，会话令牌轮换后立即在本机更新。
- 检测到房间事件序号缺口时自动重新获取完整快照，不再要求玩家返回首页。

### 修复

- 旧 WebSocket 延迟关闭时不再错误覆盖新连接状态，也不会误伤新连接上的待处理操作。
- 会话握手被拒绝时立即显示服务端错误，不再等待连接超时。
- 修正浏览器预览窗口仍显示脚手架默认标题的问题。

### 验证

- 客户端类型检查和生产构建通过。
- 通过可中断 TCP 转发注入真实网络故障，验证断线提示、退避重试、令牌轮换、完整快照
  恢复以及房间身份保持。

## [0.2.0] - 2026-07-30

### 新增

- 服务端接入 DeepSeek V4 Flash，支持 AI 题目生成、独立质量审查、主持问答、渐进提示和
  结案判断。
- DeepSeek 请求支持 JSON Schema 校验、一次格式修复、超时、并发限制以及网络错误、
  429 和 5xx 的两次退避重试。
- 新增 SQLite 私人题库、近期题目排除、管理员一次性挑战登录、短期令牌、CRUD、导入导出。
- 客户端私人题库接入真实管理员挑战与题目增删改，管理员密码不会明文传输或持久化。
- 题目编辑器支持 2 至 8 条关键事实、启用状态、输入校验和删除确认。
- 新增关闭房间、结算宽限和全员离线超时三类房间清理机制。
- 双客户端冒烟脚本覆盖同步问答、提示、结算和汤底保密。

### 变更

- 服务端和 Windows 客户端版本提升到 `0.2.0`，DeepSeek 默认模型更新为
  `deepseek-v4-flash`。
- AI 创建房间的客户端等待上限调整为 300 秒，以覆盖生成、独立质量审查和退避重试。

### 修复

- 题目生成及其格式修复连续返回无效结构时，会继续下一轮完整生成，不再提前让建房失败。
- 服务端工具脚本现在可以从仓库约定的运行方式直接加载应用包，并兼容 WSL 与 Windows
  虚拟环境。
- 私人题库房间在游戏页不再错误显示 AI 难度与风格。
- 较高的题目编辑器会在窗口内滚动，不再被小尺寸窗口裁切。

### 验证

- 59 项后端自动化测试、Ruff、格式检查、mypy 和客户端生产构建全部通过。
- 真实管理员挑战、题目创建、读取、编辑、停用、删除及私人题库开局已完成客户端联调。
- 真实 `deepseek-v4-flash` 已完成生成、独立质量审查、主持问答、渐进提示和结论判定验收。
- DeepSeek 实网模式下，两个真实 WebSocket 客户端已完成建房、加入、开局、讨论、问答、
  提示、同步结算和结算前汤底保密验收。

## [0.1.1] - 2026-07-30

### 新增

- 接入真实 FastAPI HTTP 与 WebSocket 多人服务。
- 支持真实创建房间、邀请码加入、会话恢复和完整房间快照。
- 支持玩家在线状态、聊天室、正式问题队列、撤回、公共提示、结案和房主关闭房间。
- 首页、大厅和游戏页显示真实连接状态与可读错误信息。
- 结算结果和完整作答过程仅保存在本机，不上传服务端。

### 修复

- 结算页不再自动生成演示结算，只有服务端结算事件或快照才能进入。
- 创建或加入失败时保留表单内容，避免网络波动导致输入丢失。

### 验证

- 两个客户端完成创建、加入、玩家同步、聊天室、问答、提示和同步结算全流程。
- 客户端生产构建、后端静态检查、类型检查和 34 项自动化测试全部通过。

## [0.1.0] - 2026-07-29

- 完成首版 Windows 客户端界面与本地模拟游戏流程。
- 提供深夜寝室感的现代极简布局、页面过渡动画和 NSIS 安装包。
